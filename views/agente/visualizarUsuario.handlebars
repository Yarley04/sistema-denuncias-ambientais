<div class="secao">
    <h3>Visualizar Usuário:</h3>
    <div class="card">
        <div class="card-body">
            <div class="mb-3">
                <label for="nome">Nome</label>
                <p>{{usuario.nome}}</p>
            </div>
            <div class="mb-3">
                <label for="cpf">CPF</label>
                <p>{{usuario.cpf}}</p>
            </div>
            <div class="mb-3">
                <label for="email">E-mail</label>
                <p>{{usuario.email}}</p>
            </div>

            <div class="mt-5">
                <a href="/agente/usuarios" class="btn btn-secondary">Voltar</a>
            </div>
        </div>
    </div>
</div>
